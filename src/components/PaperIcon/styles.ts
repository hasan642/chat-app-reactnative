/**
 * PaperIcon.tsx
 * developed by Hasan Alawneh.
 * A file that shows a PaperIcon styles.
 * created at: 21/12/2020
 */

import { StyleSheet } from 'react-native';

/**
 * A styles for PaperIcon.
 */
const styles = StyleSheet.create({
    icon: {
        marginLeft: 0,
        paddingLeft: 0,
        marginRight: 0,
        paddingRight: 0,
        paddingBottom: 0,
        marginBottom: 0,
        paddingTop: 0,
        marginTop: 0
    }
});

/**
 * export as default.
 */
export default styles;